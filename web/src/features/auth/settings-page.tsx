import { Card } from "../../components/ui/card";

export function SettingsPage() {
  return (
    <Card className="p-6">
      <h3 className="text-xl font-semibold">Settings</h3>
      <p className="mt-2 text-sm text-[var(--muted-foreground)]">User preferences and reminder options placeholder.</p>
    </Card>
  );
}
